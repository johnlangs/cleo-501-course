<%= form_with(model: course_prerequisite) do |form| %>
  <% if course_prerequisite.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(course_prerequisite.errors.count, "error") %> prohibited this course_prerequisite from being saved:</h2>

      <ul>
        <% course_prerequisite.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :course_id, style: "display: block" %>
    <%= form.text_field :course_id %>
  </div>

  <div>
    <%= form.label :prerequisite_id, style: "display: block" %>
    <%= form.text_field :prerequisite_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
